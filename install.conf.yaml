- clean: ['~']

- shell:
    - [git submodule sync --recursive, Installing submodules]
    - [sudo apt install zsh, Installing ZSH]
    - [sh -c "$(wget https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh -O -)", Installing OhMyZsh]

- link:
    #~/.gitconfig:
    ~/.zshrc:
      force: true
    ~/.password-store: pass
