- clean: ['~']

- link:
    #~/.gitconfig:
    ~/.zshrc:
    ~/.password-store: pass

- shell:
    - [git submodule sync --recursive, Update submodules]
    - [sh $HOME/.dotfiles/installers/ohmyzsh.sh, Install Oh-My-Zsh]

